# Feedback file name
file_name: feedback.txt

# Path to directory where repositories will be cloned 
repo_path: /afs/ir/class/cme211/git/repos  

# If true, all the feedback of the repos will be generated, else only local.
global: false

# Name of student roster file (one username per line)
roster: students.txt